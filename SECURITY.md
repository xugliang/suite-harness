# Security Policy

SuiteHarness 是面向公司服务器的 AI Agent Harness（智能体驾驭与安全运行框架）。安全问题包括但不限于跨公司/产品数据访问、授权或审批绕过、工作区逃逸、沙箱逃逸、SSRF、渠道认证绕过、MCP 越权、插件供应链绕过、密钥泄漏和审计缺失。

项目不支持个人或桌面部署。请只针对文档所述的公司服务器 + Web/飞书形态报告问题。

## Supported Versions

项目仍处于 `0.x` Alpha。当前只对最新发布的 `0.1.x` 补丁版本提供安全修复；未发布分支可能随时变化。

| Version | Supported |
| --- | --- |
| 最新 `0.1.x` | Yes |
| 更早版本 | No |

## Reporting a Vulnerability

请不要为疑似漏洞创建公开 Issue、Discussion、Pull Request，也不要在公开聊天中粘贴利用代码、密钥或客户数据。

首选使用 GitHub 仓库 **Security → Report a vulnerability** 的 Private Vulnerability Reporting（私密漏洞报告）入口。仓库维护者在首次公开发布前应确认该功能已启用。

如果该入口不可用，请通过仓库所属组织公开资料中的私密安全联系方式联系维护者，并只发送最小必要信息。不要把敏感细节发送到公开 Issue。若仓库既未启用私密报告也未公布私密联系方式，请先发送不含漏洞细节的联系请求。

报告建议包含：

- 受影响版本、提交哈希和部署形态；
- 受影响模块及前置条件；
- 可重复的最小步骤或测试；
- 预期行为与实际行为；
- 对机密性、完整性、可用性和公司/产品隔离的影响；
- 是否需要 Web、飞书、Bash、MCP、插件或特定模型配置；
- 已知缓解措施；
- 联系方式和披露时间建议。

请使用虚构的 `tenant_id`、`product-a` 和脱敏数据复现。不要测试不属于你的公司系统、模型账号、飞书应用或 MCP 服务。

维护者会尽快确认报告、复现和评估严重性；由于项目处于早期社区阶段，不承诺固定响应 SLA（服务等级协议）。在修复发布和双方协调披露前，请保持细节私密。

## Scope

通常属于范围：

- A/AB/ABC 中跨 `tenant_id` 或 `product_id` 的画像、知识、会话、工作区、MCP 或工具访问；
- 伪造 `RequestScope` 的外部输入路径；
- 绕过精确 `ToolIdentity`、短时 grant、read-only、审批或预算；
- 重放一次性审批、取消其他主体运行或重复不确定副作用；
- 飞书获得白名单外写入、任意写工具、Bash 或删除；
- `/auth/session` 公司登录态换票、WebSocket 认证、Origin、主体绑定或审批路由绕过；
- 文件目录穿越、符号链接逃逸、原子写入破坏；
- shared 工作区默认隔离或逐产品只读/读写策略绕过；
- Docker 参数注入、挂载逃逸、默认网络策略绕过，或清理失败隔离状态可被遗忘/绕过；
- `web_fetch` 访问私网/回环/链路本地或重定向 SSRF；
- MCP 跨产品连接、渠道白名单/身份漂移校验绕过、远端注解自动降权、消息边界绕过、凭据泄漏、恢复游标错投/丢失或传输校验绕过；
- 插件摘要、签名、允许路径、权限上限、原子发布或旧句柄保护绕过；
- 会话/持久化身份、CAS、幂等和审计边界错误；
- 配置/密钥解析导致敏感值泄漏。

通常不属于框架漏洞：

- 已经完全控制宿主操作系统、Docker daemon、数据库管理员或公司密钥系统后的任意行为；
- 管理员故意把恶意插件加入 `trusted_in_process` 允许清单；
- 使用文档明确标记不安全的本地开发沙箱；
- 未按部署文档实现的公司 SSO/IAM、飞书目录、代理、浏览器工作器或外部 MCP 服务自身漏洞；
- 模型回答质量、幻觉或供应商服务中断，除非能证明绕过框架安全边界；
- 只影响未支持的个人/桌面部署；
- 不包含安全影响的性能建议或一般功能请求。

即使问题来自错误部署，如果框架默认值、校验或文档可能系统性诱发风险，我们仍欢迎私密报告。

## Safe Harbor

在善意、仅测试你有权测试的系统、尽量避免数据访问和服务影响、发现后及时私密报告并遵守适用法律的前提下，项目维护者不会因该安全研究主动寻求追责。

这不是对第三方系统、客户环境、模型厂商、飞书、云服务或 MCP 服务的授权。不要执行社会工程、拒绝服务、大规模扫描、持久化访问或真实数据外传。

## Security Design

安全架构、当前生产边界和上线清单见：

- [安全模型](docs/security-model.md)
- [公司部署指南](docs/deployment.zh-CN.md)
- [架构说明](docs/architecture.md)

修复应包含回归测试；涉及公共安全契约的变化还应更新上述文档和 [CHANGELOG.md](CHANGELOG.md)。
