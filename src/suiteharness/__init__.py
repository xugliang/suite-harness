"""Composable, scoped runtime building blocks for AI agent products.

The public API is the scoped Customer Bundle kernel: ``runtime`` composes
tenant, product, and agent lifecycles; ``execution`` owns the mandatory safety
boundary; and ``memory`` provides isolated profiles with opt-in sharing.
"""

__version__ = "0.1.0"
